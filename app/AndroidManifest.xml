<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network interface access -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <!-- Hold a multicast lock for LLMNR link-local name resolution -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!--
      Background capture service.

      specialUse, not dataSync. Neither service synchronises anything: CaptureService
      runs dumpcap writing packets to a local file, and VpnCaptureService records its
      own local tunnel. dataSync is scoped to upload/download/backup/sync work and from
      Android 15 carries a six-hour daily cap, which would silently truncate a long
      capture - the one thing a capture tool must not do.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
      No storage permission. Captures and exports go to Documents/pocketpcap through
      MediaStore, which needs none, and everywhere MediaStore cannot reach - Android 10,
      or a folder outside the standard media directories - output falls back to
      app-private storage, which needs none either. READ_/WRITE_EXTERNAL_STORAGE were
      declared here until 2026-09-15; both are dangerous permissions, the app never
      asked for either at runtime, so neither was ever granted or doing anything.
    -->

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.PocketPCAP"
        android:largeHeap="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>

        <service
            android:name=".service.CaptureService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Continuous local network packet capture. The user starts a
                    capture explicitly and it must keep recording to a file while the app
                    is in the background, with a persistent notification carrying a stop
                    control. No data is transmitted off the device." />
        </service>

        <service
            android:name=".service.VpnCaptureService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:permission="android.permission.BIND_VPN_SERVICE">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Rootless local packet capture through VpnService. The tunnel
                    exists only on the device to record its own traffic to a file; it does
                    not proxy, modify or forward traffic to any remote server." />
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>

    </application>

</manifest>
